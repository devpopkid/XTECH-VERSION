{
  "name": "POPKID-KE",
  "description": "⚡ POPKID Engines — Smart, Fast, Reliable WhatsApp Bot ⚡",
  "logo": "https://files.catbox.moe/mua6vc.jpg",
  "keywords": ["popkid", "whatsapp-bot", "automation", "bot-engine"],
  "success_url": "/",
  "stack": "container",

  "env": {
    "SESSION_ID": {
      "description": "Your bot's SESSION_ID (required for authentication).",
      "required": true,
      "value": ""
    },

    "STICKER_NAME": {
      "description": "Sticker pack name (used for all stickers).",
      "required": false,
      "value": "POPKID"
    },

    "PREFIX": {
      "description": "Command prefix (leave empty only if using no prefix).",
      "required": false,
      "value": "."
    },

    "MODE": {
      "description": "Bot mode: public / private / inbox / group.",
      "required": false,
      "value": "public"
    },

    "ALWAYS_ONLINE": {
      "description": "true → bot always appears online.",
      "required": false,
      "value": "false"
    },

    "AUTO_VOICE": {
      "description": "true → convert text replies to voice automatically.",
      "required": false,
      "value": "false"
    },

    "AUTO_REPLY": {
      "description": "true → reply automatically to all messages.",
      "required": false,
      "value": "false"
    },

    "AUTO_STICKER": {
      "description": "true → auto-convert images to stickers.",
      "required": false,
      "value": "false"
    },

    "AUTO_STATUS_SEEN": {
      "description": "true → view all WhatsApp statuses automatically.",
      "required": true,
      "value": "true"
    },

    "AUTO_STATUS_REACT": {
      "description": "true → react to statuses after viewing.",
      "required": true,
      "value": "true"
    },

    "AUTO_STATUS_REPLY": {
      "description": "true → send a message after viewing a status.",
      "required": true,
      "value": "false"
    },

    "AUTO_STATUS_MSG": {
      "description": "Custom message for AUTO_STATUS_REPLY.",
      "required": true,
      "value": "*👀 Seen your status — POPKID 🖤*"
    },

    "OWNER_NAME": {
      "description": "Bot owner's name.",
      "required": false,
      "value": "popkid"
    },

    "OWNER_NUMBER": {
      "description": "Owner's WhatsApp number (with country code).",
      "required": false,
      "value": "254111385747"
    },

    "BOT_NAME": {
      "description": "Bot display name.",
      "required": false,
      "value": "POPKID"
    },

    "ANTI_LINK": {
      "description": "true → remove group invite links automatically.",
      "required": true,
      "value": "true"
    },

    "ANTI_BAD": {
      "description": "true → delete messages containing bad words.",
      "required": false,
      "value": "false"
    },

    "MENTION_REPLY": {
      "description": "true → reply when mentioned by someone.",
      "required": false,
      "value": "false"
    },

    "MENU_IMAGE_URL": {
      "description": "Image URL for bot menu & mention replies.",
      "required": false,
      "value": "https://files.catbox.moe/mua6vc.jpg"
    },

    "DESCRIPTION": {
      "description": "Bot menu caption / description.",
      "required": false,
      "value": "*⚡ POPKID Engines — The Future of WhatsApp Automation ⚡*"
    },

    "DELETE_LINKS": {
      "description": "true → delete links but keep group members.",
      "required": false,
      "value": "true"
    },

    "AUTO_RECORDING": {
      "description": "true → show recording status automatically.",
      "required": false,
      "value": "false"
    },

    "AUTO_TYPING": {
      "description": "true → show typing status automatically.",
      "required": false,
      "value": "false"
    },

    "AUTO_REACT": {
      "description": "true → react to every message.",
      "required": false,
      "value": "false"
    },

    "CUSTOM_REACT": {
      "description": "true → use emojis from CUSTOM_REACT_EMOJIS.",
      "required": false,
      "value": "false"
    },

    "CUSTOM_REACT_EMOJIS": {
      "description": "Custom emojis for reactions (comma-separated).",
      "required": false,
      "value": "💝,💖,💗,❤️‍🩹,❤️,🧡,💛,💚,💙,💜,🤎,🖤,🤍"
    },

    "ANTI_DEL_PATH": {
      "description": "'same' → resend deleted messages in same chat.",
      "required": false,
      "value": "log"
    },

    "READ_MESSAGE": {
      "description": "true → mark all messages as read instantly.",
      "required": false,
      "value": "false"
    }
  },

  "buildpacks": [
    {
      "url": "https://github.com/heroku/heroku-buildpack-nodejs.git"
    }
  ],

  "stack": "heroku-24"
}